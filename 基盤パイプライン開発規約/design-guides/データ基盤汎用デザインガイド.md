# データ基盤汎用デザインガイド

| 項目 | 内容 |
|:---|:---|
| 版 | 1.2 |
| 文書種別 | データ基盤の設計・実装・運用標準 |
| 位置づけ | GCP / BigQueryを中心とする企業データ基盤の共通設計標準 |
| 適用対象 | 業務ドメインを問わず、データ収集、変換、公開、監査、運用を行うデータ基盤 |
| 対象データ | SAP、Dynamics 365、Salesforce、イベント、BI、AI/MLデータ等 |
| 適用範囲 | 命名、データモデル、品質、権限、PII、コスト、CI/CD、DR/BCP、AI/ML利用管理 |
| 文書オーナー | 組織で指定する |
| 承認者 | データ責任者、セキュリティ責任者、基盤責任者、FinOps責任者 |

**下位文書**：本書に従うドメイン別ガイドを作成する。ドメイン別ガイドは、本書の必須要件を緩和してはならない。差異が必要な場合は、個別ガイドに理由、影響、代替統制、承認者、有効期限を記録する。

## 0. 実装環境標準

本書のTerraform、Python、Google Cloud SDK、Git、CI/CD補助ツールの推奨バージョンと必須度は、[ローカル開発環境標準](ローカル開発環境標準.md)を正とする。ローカル検証とCI/CDでは、同標準とWorkflowに定義したバージョンを一致させる。

## 1. 目的と設計原則

本書は、データ基盤を安全、再現可能、監査可能、費用予測可能な状態で開発・運用するための共通標準を定める。対象サービス、データ量、SLA、規制、業務要件に応じて実装方式を選択するが、選択理由はデータプロダクト単位で記録する。

### 1.1 規則の強度

| 区分 | 定義 | 例外の扱い |
|:---|:---|:---|
| **必須** | 本番公開前に満たす統制要件 | データ責任者、セキュリティ責任者、基盤責任者の承認が必要 |
| **標準** | 正当な理由がない限り採用する設計方式 | 不採用理由と代替策を記録する |
| **推奨** | 効果、費用、運用負荷を評価して採用する方式 | プロジェクト判断とする |
| **例** | 理解のための記述やコード例 | 本番へ無検証で適用しない |

本書の保持期間、パーティション列、クラスタリング列、BI粒度、閾値は、全資産へ一律適用する値ではない。データ量、増加率、主要クエリ、SLA、法令、保持要件に基づき決定する。

### 1.2 用語

| 用語 | 定義 |
|:---|:---|
| データオーナー | データの業務的意味、利用目的、品質基準、アクセス承認を担う責任者 |
| データスチュワード | 定義、メタデータ、品質、分類、利用ルールを管理する担当者 |
| データプロダクト | 利用者、粒度、SLA、品質、所有者を持つ公開データ資産 |
| 破壊的変更 | 既存のクエリ、BI、ML、外部連携、権限、データ意味を変更・停止させる変更 |
| PII | 個人を識別または識別可能にする情報。分類は社内規程に従う |
| 業務PIT | SCD Type 2等の有効期間に基づく指定時点照会 |
| BigQuery Time Travel | BigQueryのシステム時刻に基づく過去データ復元 |
| 冪等性 | 同じ入力と処理版を再実行しても業務データが重複・変質しない性質 |

### 1.3 基本原則

1. 原データの保存、変換、公開、監査を分離し、誰が何をいつ変更したか追跡可能にする。
2. 下流利用者には、Silverの実装資産ではなく、契約化されたGoldまたはServing資産を原則公開する。
3. 同一入力と同一処理バージョンを再実行しても、管理列を除く業務結果が一致する設計にする。
4. 品質不合格データは公開せず、Quarantineへ隔離して再処理可能にする。品質ルールごとに重大度、公開対象、フォールバック、再処理方法を定義する。
5. スキーマ、意味、粒度、権限、保持期間の変更を変更管理の対象にする。
6. データ資産には、所有者、粒度、鮮度、品質基準、問い合わせ先を付与する。説明文には、業務上の意味、1行の粒度、単位、時刻基準、更新頻度、所有者を記載する。
7. 大規模テーブルは、実際のアクセスパターンに基づいてパーティション、クラスタリング、事前集計、マテリアライズドビュー等を選択する。コストと性能は、bytes processed、実行時間、total_slot_ms、キュー待機時間などの実測値で評価する。
8. 秘密情報、暗号鍵、アクセストークンをSQL、Git、ログ、データ辞書の説明文に記載しない。

命名は第4章、時刻・金額の型は第5章に定める。

### 1.4 本書の適用限界

本書は、GCPおよびBigQueryを中心とするデータ基盤の技術・運用・ガバナンス標準である。本書は法律意見、適法性の保証、監査意見、認証を構成しない。対象データ、利用目的、本人、国・地域、業種、契約、委託先、AI用途を特定し、法務、個人情報保護責任者、セキュリティ責任者、AI責任者が適用可否を判断する。

本書単独では、個人情報保護、越境移転、著作権、営業秘密、AI規制、業界固有規制への対応は完結しない。データ分類規程、個人情報保護規程、AI利用規程、著作権・ライセンス基準、委託先管理規程、削除・保持台帳を併用する。

## 2. GCPプロジェクト、環境、リージョン

### 2.1 環境分離

開発、検証、本番は原則としてGCPプロジェクトを分離する。環境名だけをディレクトリで分ける方式は、権限分離や誤接続防止を十分に代替しない。

```text
prj-data-dev
prj-data-stg
prj-data-prod
```

SQL、ビュー定義、アプリケーションコードに本番プロジェクトIDを直接埋め込まない。

### 2.2 環境参照

| 実行環境 | 方式 |
|:---|:---|
| Dataform | `ref()`、環境変数、環境別のrelease configuration |
| dbt（採用する場合） | `ref()`、`source()`、環境別target |
| Terraform | provider、variable、環境変数、環境別ディレクトリまたはworkspace |
| 汎用SQL | CI/CDまたはテンプレート展開で環境を注入 |
| BI | 環境別接続設定。SQL本文に環境名を埋め込まない |

本書のSQL例にある`${GCP_PROJECT_ID}`はテンプレート変数である。実行前に実際のプロジェクトIDへ展開し、未展開のまま実行してはならない。

CIでは次の文字列を検出した場合にデプロイを拒否する（検査の実装例は12.3）。

```text
${GCP_PROJECT_ID}
${BQ_DATASET}
TODO
CHANGE_ME
```

### 2.3 リージョン

BigQueryデータセット、ジョブ、外部接続、BI接続、KMS鍵のロケーションを整合させる。リージョンはデータセット単位に決定し、ジョブの実行ロケーションと一致することをデプロイ前に検証する。Cloud KMSを使用する場合は、鍵のロケーションとBigQueryジョブの実行ロケーションが利用方式の要件を満たすことを確認する。リージョンの固定値をコードへ記載する場合は、対象データセットと一致することをCIまたはデプロイ前検査で確認する。

## 3. 論理データレイヤー

| レイヤー | 主な責務 | 公開方針 |
|---|---|---|
| Bronze | 原データの受領、再処理、監査、ソース仕様の保持 | 基盤運用者に限定 |
| Silver | 型変換、標準化、名寄せ、重複除去、履歴管理 | エンドユーザーへ直接公開しない |
| Gold | 業務定義済みのBI、分析、AI公開契約 | 契約と権限を満たす利用者へ公開 |
| Serving | BI、API、業務処理向けの性能・コスト最適化 | 利用目的とSLAを定義して公開 |
| Quarantine | 品質不合格、変換不能、規格外データの隔離 | 一般公開しない |
| Audit | 実行履歴、品質判定、件数、差分、再処理履歴 | 運用・監査関係者へ公開 |
| Sandbox | 期限付きの探索的分析 | 所有者、期限、削除日を必須化 |
| Restricted PII | 原PII、復号、疑似識別子生成の限定領域 | 専用サービスアカウントに限定 |

Bronzeは受領値を変更しない。`source_system`、`file_name`、`received_at`、`file_hash`、`batch_id`、`ingestion_run_id`などの受信メタデータは、業務データの変換とは区別して付与できる。再送データは上書きせず、重複排除はSilverで行う。

Silverでは型、単位、コード、履歴、重複を整える。Silverのビューは事実の統合に限定し、業務上の解釈や損益判定を含めない。GoldまたはServingでは業務ロジック、集計、利用者向け契約を提供する。

### 3.1 保持期間

保持期間は法令、契約、監査、再処理、削除要求、復旧要件、費用に基づきデータプロダクトごとに定義する。Bronze、Silver、Goldへ一律の保持日数を無検証で適用してはならない。

### 3.2 Time Travelと物理ストレージ費用

BigQuery Time Travelの保持期間はデータセットまたはプロジェクト単位で管理する。保持期間はデフォルト7日、設定可能な範囲は2日以上7日以下である（参考：[Data retention with time travel and fail-safe | BigQuery](https://cloud.google.com/bigquery/docs/time-travel)）。

頻繁にMERGE、UPDATE、DELETE、CREATE OR REPLACEを行う履歴テーブルでは、Time TravelとFail-safeによる保持データが増える可能性がある。短縮を検討する場合は、業務上必要な復旧期間とRPOを満たすこと、物理ストレージ課金モデルであること、Fail-safeの追加保持期間は設定変更できないこと、必要なスナップショットやバックアップを別途用意すること、データセット単位の設定変更が他テーブルへ与える影響を確認することを必須とする。論理ストレージ課金では、Time Travel短縮による同じ費用効果が得られない場合がある。

SCD Type 2の業務履歴とTime Travelは代替関係ではない。業務PITの保持要件はSCD履歴またはスナップショットで満たし、Time Travelは障害復旧要件として別に評価する。

## 4. 命名規則

BigQueryのデータセット、テーブル、ビュー、カラム、モデル名はASCII小文字のsnake_caseを標準とする。日本語、全角文字、空白、ハイフン、キャメルケースを使用しない。略語は組織用語集へ登録されたものだけを使用する。

### 4.1 データセット

基本形式は`[layer]_[domain_or_source]`とする。

```text
bronze_sap
bronze_salesforce
silver_customer
silver_sales
gold_sales_mart
serving_finance
quarantine_salesforce
audit_pipeline
sandbox_team_a
restricted_pii
```

データセット名だけでアクセス権を付与してはならない。IAM、Policy Tag、Row Access Policy、認可ビュー、専用テーブルなどで明示的に制御する。

### 4.2 テーブル、ビュー、特徴量

基本形式は`[prefix]_[business_entity_or_content]_[suffix]`とする。

| 接頭辞 | 用途 | 例 |
|:---|:---|:---|
| `dim_` | ディメンション、マスタ | `dim_customer` |
| `fact_` | 取引、イベント、実績 | `fact_opportunity` |
| `agg_` | 明示的な事前集計、集計マート | `agg_sales_by_customer` |
| `v_` | 利用者向け論理ビュー、解釈を含むビュー | `v_customer_pipeline` |
| `feat_` | ML特徴量 | `feat_customer_churn` |
| `stg_` | 一時変換 | `stg_opportunity_cleaned` |
| `snap_` | スナップショット | `snap_customer_daily` |

`fact_`だからパーティション必須、`dim_`だからクラスタリング必須とはしない。データ量、アクセスパターン、保持要件、実測コストに基づき決定する。

### 4.3 テーブル・ビューのサフィックス

| サフィックス | 用途 |
|:---|:---|
| `_historical`、`_history` | 履歴を保持する資産。履歴の方式（SCD Type 2、スナップショット等）を説明文に記載 |
| `_snapshot` | 特定時点の状態 |
| `_latest` | 最新状態。定義を説明文に記載 |
| `_inference` | ML推論結果 |
| `_v2` | 互換性を維持できない公開契約の新バージョン |

### 4.4 日付シャード

新規のSilver、Gold、Serving資産では、日付をテーブル名に埋め込む方式を標準としない。

```text
推奨: fact_sales を DATE(sale_at) でパーティション
非推奨: fact_sales_20260910
```

ソース受領、外部製品制約、既存移行では例外を許可する。例外には所有者、移行期限、廃止計画、代替クエリを記録する。

### 4.5 カラム名

| サフィックス・接頭辞 | 意味 | 標準型 | 例 |
|:---|:---|:---|:---|
| `_id` | 識別子 | `STRING`または要件に適した型 | `customer_id` |
| `_at` | 絶対日時 | `TIMESTAMP` | `created_at` |
| `_dt`、`_date` | 業務日付。どちらを使うかは資産内で統一する | `DATE` | `close_dt`、`target_date` |
| `_dttm` | タイムゾーンを持たない日時 | `DATETIME` | `store_open_dttm` |
| `_amt` | 金額 | `NUMERIC`または`BIGNUMERIC` | `contract_amt` |
| `_qty` | 数量 | `INT64`または`NUMERIC` | `order_qty` |
| `_cnt` | 件数 | `INT64` | `order_cnt` |
| `is_`、`has_`（接頭辞） | 真偽値 | `BOOL` | `is_closed`、`has_contract` |
| `_cd`、`_code` | コード | `STRING` | `country_cd` |
| `_json` | 半構造化データ | `JSON` | `payload_json` |
| `_score` | スコア、確率 | `FLOAT64`または`NUMERIC` | `churn_score` |
| `_embedding` | ベクトル | `ARRAY<FLOAT64>`等 | `customer_embedding` |

期間管理の適用期間を表す列名は6.1に定める。

## 5. 型、時刻、単位、意味

`TIMESTAMP`は絶対時刻として扱い、保存時はUTCを標準とする。業務日付は業務タイムゾーンを明記した`DATE`とする。各データプロダクトはタイムゾーン、日付帰属、夏時間、締め時刻をデータ契約に記載する。

BronzeとSilverでは、ソース値を無断で丸めたり、欠損を0へ置換したりしない。変換不能値は原値または監査可能な表現を保持し、Quarantineへ隔離する。

### 5.1 金額

確定金額には`NUMERIC`または`BIGNUMERIC`を使用する。金額列には、通貨コード、税込・税抜・不課税の区分、符号、小数桁、丸め規則、為替換算、返品・取消・値引きの扱いをデータ辞書へ記載する。

確定した財務・請求・売上金額に`FLOAT64`を使用してはならない。予測値や統計値で近似値が適切な場合は`FLOAT64`を使用できる。

### 5.2 NULL

NULL、空文字、未知、未設定、該当なし、論理削除済みを混同してはならない。各列の説明文にNULLの意味と入力不能時の処理を記載する。

### 5.3 STRUCT、ARRAY、JSON

1. Bronzeでは、ソース原文のネスト構造を監査・再処理目的で保持してよい。
2. Silver以降では、頻繁に検索、JOIN、集計するJSONキーを物理カラムへ切り出す。
3. ネストは原則2階層以内とする。超える場合は利用目的、アクセス方法、性能テストを記録する。
4. ARRAYを`UNNEST`する場合は、行数増加と結果粒度を説明文へ記載する。
5. 金額と件数を集計する場合、親行と子行の重複計上をテストする。
6. BI向けGold資産では、ARRAYと未加工JSONを原則として直接公開しない。
7. APIやデータサイエンス向けのネスト公開は、利用者、粒度、クエリ例を契約へ記載する。

## 6. 履歴管理とスキーマ進化

### 6.1 履歴管理（SCD Type 2）

SCD Type 2を採用する場合、適用期間、境界規則、業務キー、重複・隙間の扱い、終端値を資産単位で定義する。`start_date`と`end_date`は適用期間専用の列名とし、同一業務キーの期間重複を禁止する。`9999-12-31`を有効行の終端に使用する場合は、その規則を資産契約へ記載する。

適用期間の列は、期間の精度に応じて次のとおり使い分ける。

| 期間の精度 | 列名 | 型 | 境界規則 | 終端値 |
|:---|:---|:---|:---|:---|
| 日単位で切り替わる適用期間（約款、単価、契約、マスタの版など） | `start_date`、`end_date` | `DATE` | 開始日・終了日とも含む | `9999-12-31` |
| 時刻の精度が必要な有効期間 | `valid_from_at`、`valid_to_at` | `TIMESTAMP`（UTC） | 開始を含み、終了を含まない | `9999-12-31`またはNULL。データプロダクト単位で統一する |

同一資産内で両方式を混在させない。期間管理する資産の主キーは、業務キーと適用開始の組とし、業務キーは改定時にも変更しない。

SCD Type 2の業務PITとBigQuery Time Travelを混同してはならない（3.2）。

### 6.2 業務PIT照会

```sql
DECLARE target_timestamp TIMESTAMP DEFAULT TIMESTAMP('2025-12-31 23:59:59+00');

SELECT
  customer_id,
  COUNT(DISTINCT contract_id) AS historical_active_contract_cnt,
  SUM(contract_amt) AS historical_total_contract_amt
FROM `${GCP_PROJECT_ID}.silver_sales.fact_contract_history`
WHERE valid_from_at <= target_timestamp
  AND valid_to_at > target_timestamp
GROUP BY customer_id;
```

このクエリはSCD Type 2の業務有効期間を使う。`is_current`だけで過去状態を復元してはならない。

**境界条件**

- `valid_from_at < valid_to_at`（`start_date`／`end_date`の場合は`start_date <= end_date`）
- 同一`contract_id`の有効期間が重複しない
- 同一`contract_id`で`is_current = TRUE`が最大1件
- 境界時刻で`valid_from_at`を含み、`valid_to_at`を含まない規則を統一する
- 未来開始、削除済み、訂正、NULL終端の扱いを定義する
- パーティション削減効果をdry runと実行計画で確認する

### 6.3 変更分類

列追加、説明更新、列名変更、型変更、列削除、粒度変更、PII分類変更を分類し、破壊的変更には依存調査、通知、互換資産、移行期限、削除記録を要求する。変換不能値を黙ってNULLに変換しない。

| 変更 | 分類 | 対応 |
|:---|:---|:---|
| NULL許容列の追加 | 後方互換 | 依存確認後に追加 |
| 説明文の更新 | 通常変更 | 意味が変わらないことを確認 |
| 列名変更 | 破壊的 | 互換列、互換ビュー、移行期間 |
| 型変更 | 原則破壊的 | 変換、併存、移行テスト |
| 列削除 | 破壊的 | 利用調査、通知、期限、削除 |
| 粒度変更 | 破壊的 | 新資産または新バージョン |
| PII分類引き上げ | 権限変更 | 権限、BI、エクスポートを再確認 |

### 6.4 廃止手順

1. カタログ、クエリ履歴、Dataform、dbt、BI、MLの依存関係を調査する。
2. 非推奨、廃止予定日、移行先を登録する。
3. 互換ビューまたは代替列を提供する。
4. 利用者へ通知し、移行状況を確認する。
5. 期限経過後に削除し、変更記録を保存する。

列名を`deprecated_`へ変更すること自体が破壊的変更になるため、名前変更だけで廃止管理を完了したとみなしてはならない。

### 6.5 変換不能値

変換不能値を黙ってNULLへ変換してはならない。原値または監査可能な表現を保持し、Quarantineへ隔離し、原因、件数、再処理方法を記録する。

## 7. パーティション、クラスタリング、コスト

### 7.1 パーティション選定

パーティションの採否を固定サイズ閾値だけで決めない。テーブル規模、増加率、主要クエリの時間範囲、パーティションごとのデータ量、更新頻度、バックフィル、保持期限、BIフィルター、実測コストを評価する。

- テーブル規模と増加率
- 主要クエリの時間範囲
- パーティションごとのデータ量
- 更新頻度とバックフィル方法
- 保持期限と削除要件
- BIツールがフィルターを生成できるか

`fact_`という名前だけを理由に、特定のパーティション列を割り当ててはならない。

`fact_`という名前だけを理由に、特定のパーティション列を割り当ててはならない。

### 7.2 `require_partition_filter`

大規模な時間系テーブルでは`require_partition_filter = TRUE`を標準とする。直接SQL、認可ビュー、BI、増分更新、バックフィル、フルリフレッシュ、品質テスト、復旧処理で利用できることを検証する。

小規模、全期間走査が主目的、管理専用、適切なパーティション列がない場合は例外を認める。例外には理由、代替コスト制御、承認者を記録する。

BigQueryは、パーティション列に適切な条件がある場合、該当パーティションを削減できる。これはdry runと実行計画で検証する（参考：[Introduction to partitioned tables | BigQuery](https://docs.cloud.google.com/bigquery/docs/partitioned-tables)）。

### 7.3 増分更新とバックフィル

`require_partition_filter = TRUE`を設定したテーブルでは、Dataform、dbt、Composer、Workflows等が発行する全クエリに対象パーティション条件が入ることを検証する。増分更新、特定期間のバックフィル、フルリフレッシュ、失敗後の再実行を別々にテストする。

dbtを採用し、BigQueryアダプタを使用する場合、`_dbt_max_partition`等の機能や変数を採用するかどうかを、使用中のdbtバージョンの公式仕様で確認する。名称や挙動をバージョン非依存の標準として扱ってはならない。CIでは、生成SQLに対象パーティション条件があること、対象外の全期間走査がないこと、バックフィル範囲が意図した日付に限定されること、`require_partition_filter`による予期しないエラーがないこと、フルリフレッシュが承認済み手順としてのみ実行されることを確認する。

### 7.4 フィルターの書き方

パーティション列への条件は、対象パーティションを削減できる形で記述する。関数使用の可否をすべてのクエリに対する絶対禁止事項として扱わず、パーティション方式、クエリ形状、ビュー展開、BI生成SQLを対象に検証する。

パーティションプルーニングとクラスタリングによるブロックプルーニングは別の最適化である。両者を混同してはならない。

### 7.5 クラスタリング

クラスタリング列と列順は、頻繁なフィルター、期間・テナント・業務単位、JOIN、GROUP BY、ORDER BY、データ分布、NULL率、実行計画を基に決定する。高カーディナリティ順を一律ルールとしない。採用後はbytes processed、レイテンシ、total_slot_msで効果を検証する。

クラスタリング列は、頻繁なJOIN、等価フィルター、集計に使う列から選ぶ。採否、列順、列数、効果をbytes processedとレイテンシで検証する。BigQueryの利用上限や対象エディションを、実行環境で確認する。

**複数クラスタリング列の順序**

複数列を指定する場合、左から右の順序がストレージブロックの並びとクエリ最適化に影響する。順序は、単なるカーディナリティの高さだけで決めず、次の優先順位で決定する。

1. 単独または複合条件で頻繁にフィルターされる列。
2. 主要な期間、テナント、業務単位など、クエリの選択性を高める列。
3. JOIN、GROUP BY、ORDER BYで繰り返し使用される列。
4. データ分布、NULL率、更新方式、実行計画に基づく実測効果。

最初の列だけが使われるクエリと、複数列を先頭から順に使うクエリを分けてベンチマークする。高カーディナリティ列を常に最初に置くという一律ルールは採用しない。クラスタリング列は最大4列までとし、実際のBigQuery仕様と利用環境を確認する（参考：[Introduction to clustered tables | BigQuery](https://cloud.google.com/bigquery/docs/clustered-tables)）。

### 7.6 コスト統制

コスト統制として、CIのdry run、代表クエリのベースライン比較、ジョブラベル、BI期間フィルター、クエリ・ストレージ・Reservation費用の分離、費用急増アラートを実装する。

- CIで代表クエリをdry runする。
- bytes processedと実行時間をベースラインと比較する。
- ジョブラベルに環境、チーム、データプロダクトを付与する。
- BIの期間フィルターと粒度をデータ契約に含める。
- クエリ、ストレージ、予約・スロット費用を分けて監視する。
- 費用急増時のアラート、所有者、停止・制限手順を定義する。

予算、費用配賦、クエリ上限などの統制項目は17.2に定める。

### 7.7 Slot、Edition、Reservation

bytes processedだけでは、予約型BigQueryの計算資源消費と待機を十分に評価できない。ステージング環境では、代表クエリと代表ワークロードについて、`total_slot_ms`またはJob統計、実行時間、キュー待機時間、シャッフル量、失敗率、Reservation、割り当てプロジェクト、リージョン、Edition、BI同時実行時のスロット使用量を記録する。

本番では、ELT、BI、データサイエンス、管理処理を必要に応じて別Reservationへ分離し、プロジェクトまたはフォルダの割り当てを管理する（参考：[Understand reservations | BigQuery](https://cloud.google.com/bigquery/docs/reservations-workload-management)）。

同時実行数やプロジェクト単位のスロット上限を設定する場合は、利用可能な機能の提供段階、リージョン、Edition、組織ポリシーを確認する。Preview機能を必須統制にしてはならない。

## 8. Dataform、SQL、Stored Procedure、UDF

Dataformを標準のSQLモデル、依存関係、compile、assertion管理ツールとする。ただし、複雑な業務計算、対象パーティション単位の再生成、検算、トランザクション性、再実行制御はBigQuery Stored Procedureを使用できる。再利用可能な検証・変換はUDFで実装できる。採用理由と責務境界を資産単位で記録する。

### 8.1 DDLの実行条件

8.2〜8.4はGoogleSQLのテンプレートである。`${GCP_PROJECT_ID}`は実行前にCI/CDで置換する。置換後のSQLをdry runし、テスト用データセットへ適用してから本番へデプロイする。

本番での無計画な`CREATE OR REPLACE TABLE`を禁止する。列削除、型変更、Policy Tag削除、権限変更を伴う場合は、変更レビューと移行計画を必須とする。

### 8.2 ディメンションの例

```sql
CREATE TABLE IF NOT EXISTS `${GCP_PROJECT_ID}.silver_customer.dim_customer`
(
  customer_id STRING OPTIONS(description = '統合顧客一意ID'),
  customer_name STRING OPTIONS(description = '顧客正式名称。PIIポリシータグの保護対象'),
  industry_cd STRING OPTIONS(description = '業種コード'),
  country_cd STRING OPTIONS(description = '国コード'),
  source_system_cd STRING OPTIONS(description = '主たるデータソース'),
  source_updated_at TIMESTAMP OPTIONS(description = 'ソース側最終更新日時UTC'),
  is_source_deleted BOOL OPTIONS(description = 'ソース側論理削除フラグ'),
  ingestion_run_id STRING OPTIONS(description = 'パイプライン実行ID'),
  loaded_at TIMESTAMP OPTIONS(description = 'BigQueryへのロード日時UTC'),
  pipeline_version STRING OPTIONS(description = '処理コードまたはモデルのバージョン')
)
CLUSTER BY customer_id
OPTIONS (
  description = '統合顧客ディメンション。利用者向け公開はGoldまたはServing経由とする。'
);
```

`CLUSTER BY customer_id`は、主要クエリとデータ量で効果を確認した上で採用する。効果がない場合は削除してよい。

### 8.3 日付パーティション付きファクトの例

```sql
CREATE TABLE IF NOT EXISTS `${GCP_PROJECT_ID}.silver_sales.fact_opportunity`
(
  opportunity_id STRING OPTIONS(description = '商談一意ID'),
  customer_id STRING OPTIONS(description = '統合顧客ID'),
  opportunity_name STRING OPTIONS(description = '商談名'),
  stage_cd STRING OPTIONS(description = '商談フェーズコード'),
  opportunity_amt NUMERIC OPTIONS(description = '商談金額'),
  currency_cd STRING OPTIONS(description = '金額の通貨コード'),
  close_dt DATE OPTIONS(description = '商談完了予定日または完了日。パーティション列'),
  is_won BOOL OPTIONS(description = '受注フラグ'),
  is_closed BOOL OPTIONS(description = 'クローズフラグ'),
  source_system_cd STRING OPTIONS(description = 'データソース'),
  source_updated_at TIMESTAMP OPTIONS(description = 'ソース側最終更新日時UTC'),
  is_source_deleted BOOL OPTIONS(description = 'ソース側論理削除フラグ'),
  ingestion_run_id STRING OPTIONS(description = 'パイプライン実行ID'),
  loaded_at TIMESTAMP OPTIONS(description = 'BigQueryへのロード日時UTC'),
  pipeline_version STRING OPTIONS(description = '処理コードまたはモデルのバージョン')
)
PARTITION BY close_dt
CLUSTER BY customer_id
OPTIONS (
  require_partition_filter = TRUE,
  description = '商談ファクト。close_dtによるパーティションフィルターを要求する。'
);
```

### 8.4 SCD Type 2履歴の例

```sql
CREATE TABLE IF NOT EXISTS `${GCP_PROJECT_ID}.silver_sales.fact_contract_history`
(
  contract_history_id STRING OPTIONS(description = '履歴レコードのサロゲートキー'),
  contract_id STRING OPTIONS(description = '契約番号。ナチュラルキー'),
  customer_id STRING OPTIONS(description = '統合顧客ID'),
  contract_start_dt DATE OPTIONS(description = '契約開始日'),
  contract_end_dt DATE OPTIONS(description = '契約終了日'),
  contract_amt NUMERIC OPTIONS(description = '契約金額'),
  currency_cd STRING OPTIONS(description = '金額の通貨コード'),
  plan_cd STRING OPTIONS(description = '契約プランコード'),
  valid_from_at TIMESTAMP OPTIONS(description = '有効開始日時UTC'),
  valid_to_at TIMESTAMP OPTIONS(description = '有効終了日時UTC'),
  is_current BOOL OPTIONS(description = '現在有効なレコードであることを示す'),
  source_system_cd STRING OPTIONS(description = 'データソース'),
  source_updated_at TIMESTAMP OPTIONS(description = 'ソース側最終更新日時UTC'),
  is_source_deleted BOOL OPTIONS(description = 'ソース側論理削除フラグ'),
  ingestion_run_id STRING OPTIONS(description = 'パイプライン実行ID'),
  loaded_at TIMESTAMP OPTIONS(description = 'BigQueryへのロード日時UTC'),
  pipeline_version STRING OPTIONS(description = '処理コードまたはモデルのバージョン')
)
PARTITION BY DATE(valid_from_at)
CLUSTER BY customer_id, contract_id
OPTIONS (
  require_partition_filter = TRUE,
  description = 'SCD Type 2契約履歴。業務PIT照会の期間条件を品質テストで検証する。'
);
```

時刻の精度が必要な有効期間の例である。日単位の適用期間は`start_date`／`end_date`（`DATE`）を使う（6.1）。`valid_to_at`の終端を`9999-12-31`とするかNULLとするかは、データプロダクト単位で統一する。

### 8.5 コード例と実装コードの管理

具体的なSQL、YAML、Terraform、Workflowsコードは、製品バージョンと実行環境を固定したImplementation Guideで管理する。本書の例を未検証のまま本番へ適用しない。

本書のSQLは実装標準例である。実行用のSQL、dbt、Dataform、Terraform、Workflowsは、対象バージョンを固定したリポジトリへ保存し、CIで検証した成果物だけを本番へ適用する。本書へ新たなコード例を追加する場合も、構文、権限、リージョン、入力データ、期待結果をテストへ含める。

## 9. 外部データ連携と冪等性

### 9.1 外部データ連携

DTS、Cloud Run Jobs、Cloud Functions、Pub/Sub、Eventarcの採用は、ソースの仕様、データ量、API制限、完了イベント、再送、遅延、認証方式で決める。取り込み方式、責任分界、再実行方法をデータ契約に記録する。

### 9.2 冪等性と再実行

冪等性はデータ特性別に設計する。最新状態は業務キーによるMERGE、再生成可能な日付ファクトは対象パーティション再生成、追記専用イベントはイベントIDによる重複排除、監査ログはappend-only、履歴はSCD検証を標準とする。実行ID、入力範囲、対象資産、コードバージョンを永続化する。

| データ特性 | 推奨方式 |
|:---|:---|
| 最新状態ディメンション | ビジネスキーを使うMERGE |
| 再生成可能な日付ファクト | 対象パーティションの置換または再生成 |
| 追記専用イベント | 一意なイベントIDによる重複排除付きappend |
| 監査ログ | append-only、保持、重複、改ざん検知を定義 |
| 履歴 | SCD Type 2と期間整合性検証 |
| 集計 | 入力範囲と集計キーを固定して再生成またはMERGE |

`INSERT INTO`を一律禁止しない。append-only資産には、イベントID、再送、遅延到着、削除イベント、保持期間を定義する。

同じ入力スナップショットと処理バージョンを再実行した場合、実行時刻等の管理列を除き、出力行集合と業務値が一致することを検証する。

### 9.3 非同期BigQueryジョブ

**状態判定**

BigQuery Job APIの状態は、次の3状態で判定する。`errorResult`は最終エラー、`errors[]`は実行中に発生したエラー一覧である（参考：[Job resource | BigQuery REST API](https://cloud.google.com/bigquery/docs/reference/rest/v2/Job)）。

| 条件 | 処理 |
|:---|:---|
| `state != DONE` | 最大待機時間、最大試行回数、指数バックオフを適用して継続 |
| `state == DONE`かつ`errorResult`あり | 失敗。後続公開を停止 |
| `state == DONE`かつ`errorResult`なし | 成功。品質検証へ進む |

**二重発行防止**

再試行時は、実行ID、入力範囲、対象テーブル、コードバージョンを永続化する。同一実行IDが成功済みの場合は再発行しない。ジョブIDだけを冪等性キーとみなしてはならない。

Workflows等の具体的なYAMLは、採用製品のバージョンを固定した別のImplementation Guideで管理し、本書へ未検証のコードを埋め込まない。

## 10. データ品質と公開ゲート

品質ルールには、重大度、公開対象、フォールバック、再処理方法、通知先、所有者を定義する。必須項目、NULL率、主キー重複、参照整合性、許容値、範囲、件数差分、SCD期間、パーティション欠落、単価未解決、冪等性、ファンアウトを検証する。

Critical品質エラーがある場合、新しいGoldまたはServingを公開しない。前回正常版を維持し、DQ結果、対象範囲、原因、再処理状況をAuditへ記録する。Warningは警告表示付き公開を許可できるが、承認されたルールであることを要する。

### 10.1 重大度

| 重大度 | 例 | 失敗時動作 |
|:---|:---|:---|
| Critical | 主キー重複、金額二重計上、PII漏えい | 公開停止、アラート、承認必須 |
| High | 更新遅延、件数急減、SCD期間重複 | 前回正常版を維持し、再処理 |
| Medium | NULL率上昇、未知コード増加 | 警告、公開継続を担当者判断 |
| Low | 軽微な表記揺れ | 隔離または次回改善 |

### 10.2 Dataform・dbtのテスト

Dataformではassertionで品質テストを定義し、CIでcompileとassertionを実行する。dbtを採用する場合は、以下のようにテストを定義する。

次はdbt Coreと`dbt_utils`を使用する場合の例である。採用するdbtとパッケージのバージョンを固定し、CIで`dbt parse`、`dbt compile`、`dbt test`を実行する。

```yaml
version: 2

models:
  - name: fact_opportunity
    description: "全社商談ファクトテーブル。1行=1商談。"
    columns:
      - name: opportunity_id
        description: "商談一意ID"
        tests:
          - unique
          - not_null

      - name: customer_id
        description: "統合顧客ID"
        tests:
          - not_null

      - name: opportunity_amt
        description: "商談金額"
        tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: "opportunity_amt >= 0"

      - name: stage_cd
        description: "商談フェーズコード"
        tests:
          - accepted_values:
              values:
                - lead
                - qualification
                - proposal
                - negotiation
                - won
                - lost
```

### 10.3 BigQuery SQLテスト

各クエリは、異常がない場合に0行を返すことを期待する。

```sql
-- 主キー重複
SELECT
  opportunity_id,
  COUNT(*) AS row_count
FROM `${GCP_PROJECT_ID}.silver_sales.fact_opportunity`
GROUP BY opportunity_id
HAVING COUNT(*) > 1;
```

```sql
-- SCD Type 2の期間逆転
SELECT
  contract_history_id,
  valid_from_at,
  valid_to_at
FROM `${GCP_PROJECT_ID}.silver_sales.fact_contract_history`
WHERE valid_from_at >= valid_to_at;
```

```sql
-- SCD Type 2の現在レコード重複
SELECT
  contract_id,
  COUNTIF(is_current) AS current_count
FROM `${GCP_PROJECT_ID}.silver_sales.fact_contract_history`
GROUP BY contract_id
HAVING COUNTIF(is_current) > 1;
```

```sql
-- 商談金額の負数
SELECT
  opportunity_id,
  opportunity_amt
FROM `${GCP_PROJECT_ID}.silver_sales.fact_opportunity`
WHERE opportunity_amt < 0;
```

Great Expectations等のPython実装は、本文へ未検証のコードを埋め込まず、採用バージョン、Data Context、Datasource、Checkpointを固定したリポジトリで管理する。「完全準拠」「ランタイムエラーを完全排除」といった断定は、実行環境のCI結果がある場合だけ使用する。

### 10.4 ファンアウトとBI

複数ファクトを無計画に同時JOINしてはならない。

```text
非推奨:
  dim_customer
    ├─ fact_opportunity
    └─ fact_contract

推奨:
  dim_customer
    ├─ agg_opportunity_by_customer
    └─ agg_contract_by_customer
```

子テーブルを同じ粒度へ事前集約してからJOINする。BI側の`COUNT(DISTINCT)`だけでファンアウトを隠蔽してはならない。

BIツールは、原則としてGoldまたはServingだけを参照する。Silver、未集約ARRAY、未加工JSONを直接Exploreやデータソースへ公開しない。

## 11. PII、IAM、監査

PII分類は初期段階から実施する。原PIIは`restricted_pii`等へ分離し、一般BI利用者へ直接公開しない。Policy Tag、動的データマスキング、Row Access Policy、認可ビュー、専用テーブルを組み合わせる。単純なSHA-256を匿名化や安全なマスキングとみなさない。

### 11.1 基本方針

カラム名の`_pii_high`や`_pii_med`は補助的な分類であり、アクセス制御ではない。強制制御はPolicy Tag、IAM、Row Access Policy、認可ビュー、専用テーブル等で行う。BigQueryでは列レベルアクセス制御と動的データマスキングを組み合わせられる（参考：[Introduction to column-level access control | BigQuery](https://cloud.google.com/bigquery/docs/column-level-security-intro)）。

### 11.2 保護方式

| 目的 | 標準方式 |
|:---|:---|
| 表示だけ隠す | Policy Tag、動的データマスキング、認可ビュー |
| 同一人物を照合する | 管理鍵付きHMACまたは決定論的暗号化 |
| 原値へ戻す | AEAD暗号化またはトークン化 |
| 外部連携 | 契約済みトークン化または疑似識別子 |
| 集計公開 | 集計、少数セル抑制、必要に応じた匿名化 |

単純なSHA-256をPIIの安全なマスキングまたは匿名化として扱ってはならない。氏名、メール、電話番号は候補値が限定され、辞書攻撃を受けやすい。

### 11.3 原PIIの分離

原PIIは`restricted_pii`等の専用データセットへ分離する。一般BI利用者やアナリストへ原PIIを直接公開してはならない。復号処理は専用サービスアカウントまたは限定された処理経路へ閉じ込める。

### 11.4 疑似識別子の生成

Goldの利用者クエリで原PIIを読み、暗号化する設計を標準としてはならない。疑似識別子は、制限されたETLまたは専用サービスで事前生成し、Goldは疑似識別子だけを参照する。

```text
restricted_pii
  └─ PII原値
       └─ 制限されたETL / 専用サービス
            └─ HMACまたは決定論的暗号化
                 └─ customer_pseudonym
                      └─ Goldビュー
```

疑似識別子は、用途、鍵バージョン、正規化方式バージョンを管理する。同じ入力が同じ出力になる方式は頻度情報と同一性を漏らすため、公開範囲を限定する。

### 11.5 暗号化キー

暗号鍵または平文keysetをSQL、Git、一般テーブル、ログ、BIへ保存してはならない。BigQuery AEAD関数を使用する場合は、Cloud KMSで保護したkeyset、実行主体、鍵ローテーション、復号権限、監査ログを別途設計する（参考：[AEAD encryption functions | BigQuery](https://cloud.google.com/bigquery/docs/reference/standard-sql/aead_encryption_functions)）。

Goldビューは鍵テーブルを直接参照せず、事前生成済みの疑似識別子テーブルを参照する。

### 11.6 マスキング列のJOIN

動的データマスキングで置換された列を、下流クエリのJOINキーとして使用してはならない。NULL、既定値、一般化値、方式の異なるハッシュ値は、同一人物の照合キーとして保証されず、誤結合、未結合、同一値への集中によるファンアウトを引き起こす可能性がある。

JOINが必要な場合は、用途、鍵バージョン、正規化方式を管理した事前生成済みの`customer_pseudonym`等を使用する。決定論的マスキングや決定論的暗号化をJOINに使用する場合も、同じ鍵、コンテキスト、正規化方式を利用することを契約へ記録し、承認済みの組み合わせだけを許可する。

### 11.7 IAMと監査

人間の利用者とサービスアカウントを分離する。Terraform、Dataform、Workflows、Cloud Run、Cloud Functions、DQ、BI抽出、PII処理のサービスアカウントを必要に応じて分離し、一般利用者へ`roles/bigquery.admin`を付与しない。PII参照、権限変更、エクスポート、コピー、削除を監査する。

- 一般利用者へ`roles/bigquery.admin`を付与しない。
- 人間の利用者とパイプラインサービスアカウントを分離する。
- サービスアカウントは必要なデータセット、テーブル、ジョブ操作だけを許可する。
- PII列へPolicy Tag等を設定する。
- Row Access Policyを使う場合は、Lookupテーブル、BI、性能、互換性を検証する（参考：[Introduction to row-level security | BigQuery](https://docs.cloud.google.com/bigquery/docs/row-level-security-intro)）。
- 原PII、疑似識別子、復号権限、エクスポート権限を分離する。
- PII参照、権限変更、エクスポート、コピー、削除を監査ログで監視する。

### 11.8 監査メタデータ

| 列 | 意味 |
|:---|:---|
| `source_system_cd` | 元システム |
| `source_record_id` | ソースレコードID |
| `source_updated_at` | ソース側更新日時 |
| `is_source_deleted` | ソース削除フラグ |
| `ingestion_run_id` | パイプライン実行ID |
| `ingestion_batch_id` | 取り込みバッチID |
| `loaded_at` | ロード日時UTC |
| `pipeline_version` | 処理コードまたはモデルの版 |
| `record_hash` | 変更検知用ハッシュ。PIIを直接含めない |

`bq_job_id`を保持する場合は補助的な追跡情報とする。複数ジョブで構成される処理の主キーには`ingestion_run_id`を使用する。

### 11.9 Secretと構成管理の状態ファイル

TerraformではSecretの実値を変数やstateへ保存しない。TerraformはSecretのメタデータと参照権限を管理し、Secret Versionの登録は承認済みCI/CDまたは運用手順で行う。stateバケットはアクセス制御、監査、暗号化、保持を設定する。

## 12. IaC、CI/CD、変更管理

### 12.1 IaCと変更管理

データセット、テーブル、ビュー、ルーティン、パーティション、クラスタリング、Policy Tag、IAM、Row Access Policy、監視、アラートを承認済みIaCで管理する。本番手動DDLは原則禁止し、緊急変更は事後にコードへ反映して環境差分を解消する。

Terraform等の承認済みIaCで、次の主要リソースを管理する。

- データセット
- テーブルとスキーマ
- ビューとルーティン
- パーティションとクラスタリング
- Policy Tagとデータポリシー
- IAM
- Row Access Policy
- 監視、アラート、ログ設定

### 12.2 CI/CD

本番マージまたはデプロイ前に、次を自動実行する。

- SQL lintとフォーマット
- BigQuery dry runまたは構文検証
- Dataform compileとassertion
- dbt `parse`、`compile`、`test`（dbtを採用する場合）
- PII分類と公開差分の検査
- プレースホルダー残存検査
- Terraform `fmt`、`validate`、`plan`
- 破壊的変更と依存関係の検査
- 代表クエリのbytes processed比較
- 冪等性テスト
- Critical品質失敗時にGold公開を止めるゲート
- IAMの過剰権限検査
- Organization Policyの適用検査
- Policy Tag、Row Access Policy、公開設定の差分検査
- Secret、鍵、PIIの静的スキャン
- 依存脆弱性の検査（SCA、SAST、Secret scan）
- コンテナ脆弱性、Digest、署名検査
- ラベル欠落検査
- `maximum_bytes_billed`の設定検査
- 代表クエリのコスト回帰検査
- パーティション条件の生成SQL検査
- 破壊的変更と削除波及の検査
- 予算・費用上限に関する運用設定検査

実行例を本文へ追加する場合は、対象製品のバージョン、必要権限、入力テーブル、検証方法を併記する。未検証コードを「プロダクションコード」や「完全準拠」と表現してはならない。

### 12.3 プレースホルダー検査

```bash
if grep -R -n -E '\$\{GCP_PROJECT_ID\}|\$\{BQ_DATASET\}|TODO|CHANGE_ME' .; then
  echo 'Unresolved placeholder found' >&2
  exit 1
fi
```

この検査を本番デプロイ前のCIへ組み込む。SQL、鍵、PIIをCIログへ出力してはならない。

### 12.4 DDLの配備手順

1. `${GCP_PROJECT_ID}`を対象環境のプロジェクトIDへ展開する。
2. 対象データセットのリージョンとジョブのロケーションを確認する。
3. BigQuery dry runで構文と参照先を確認する。
4. テスト用データセットへ適用する。
5. パーティション、クラスタリング、権限、Policy Tagを検証する。
6. CIの承認後に本番へデプロイする。

## 13. 運用、SLO、DR / BCP

### 13.1 運用とSLO

データプロダクトごとに鮮度SLA、実行時間SLO、品質SLO、RPO、RTO、通知先、エスカレーション、Runbook、再処理方法を定義する。BigQuery Jobの状態判定と再試行は9.3による。

### 13.2 保持、DR、BCP、バックアップ

保持期間は法令、契約、監査、削除要求、再処理、費用、RPO / RTOで資産単位に決める。Time Travelは障害復旧用であり、業務PITやSCD履歴の代替ではない。バックアップには、データだけでなくIAM、Policy Tag、Row Access Policy、鍵、BI接続、下流連携を含める。定期的な復元テストを必須とする。

業務重要度ごとにRPO、RTO、復旧方法、責任者を定義する。バックアップの存在だけでなく、定期的な復元テストを必須とする。

復元テストでは、次を確認する。

- 指定時点まで復元できる
- IAM、Policy Tag、Row Access Policyが復元される
- BIと下流連携が再接続できる
- 重複なく再処理できる
- RPO/RTOを満たす
- 監査証跡を追跡できる

## 14. データ契約

### 14.1 契約項目

GoldまたはServingの公開資産には、少なくとも以下を登録する。

| 契約項目 | 内容 |
|---|---|
| 粒度 | 1行が表す業務単位 |
| 主キー | 一意性の定義 |
| 対象期間 | データの時間範囲と時刻基準 |
| 更新 | 更新頻度、方式、鮮度SLA、取り込み方式と責任分界、再実行方法 |
| 意味 | 業務定義、単位、NULLと削除済みの扱い |
| 金額 | 通貨、税、丸め、返品・取消の扱い |
| 利用 | 利用可能な結合キー、禁止される結合パターン、コスト上の推奨フィルター、BIの期間フィルターと粒度 |
| 品質 | 必須検査、重大度、公開条件 |
| 責任 | オーナー、スチュワード、問い合わせ先 |
| セキュリティ | 分類、権限、エクスポート可否 |

Goldは利用者向け契約面であり、実体を論理ビューに限定しない。性能、費用、鮮度、更新方式に応じてテーブル、ビュー、マテリアライズドビュー、集計テーブルを選択する。

### 14.2 期間限定ビューの例

```sql
CREATE OR REPLACE VIEW `${GCP_PROJECT_ID}.gold_sales_mart.v_customer_pipeline_last_365_days`
OPTIONS (
  description = '顧客別未クローズ商談。Asia/Tokyoの当日から過去365日。1行=1顧客。'
)
AS
SELECT
  c.customer_id,
  p.customer_pseudonym,
  c.industry_cd,
  COUNT(o.opportunity_id) AS open_opportunity_cnt,
  COALESCE(SUM(o.opportunity_amt), NUMERIC '0') AS total_pipeline_amt,
  COALESCE(
    SUM(IF(o.stage_cd = 'proposal', o.opportunity_amt, NUMERIC '0')),
    NUMERIC '0'
  ) AS proposal_stage_amt,
  COALESCE(
    SUM(IF(o.stage_cd = 'negotiation', o.opportunity_amt, NUMERIC '0')),
    NUMERIC '0'
  ) AS negotiation_stage_amt
FROM `${GCP_PROJECT_ID}.silver_customer.dim_customer` AS c
LEFT JOIN `${GCP_PROJECT_ID}.silver_customer.customer_pseudonym` AS p
  ON c.customer_id = p.customer_id
LEFT JOIN `${GCP_PROJECT_ID}.silver_sales.fact_opportunity` AS o
  ON c.customer_id = o.customer_id
  AND o.is_closed = FALSE
  AND o.close_dt >= DATE_SUB(CURRENT_DATE('Asia/Tokyo'), INTERVAL 365 DAY)
GROUP BY
  c.customer_id,
  p.customer_pseudonym,
  c.industry_cd;
```

このビューは原PIIと暗号鍵を参照しない。`customer_pseudonym`は、制限されたETLまたは専用サービスで事前生成されていることを前提とする。

`CURRENT_DATE('Asia/Tokyo')`を使用するため、実行日によって結果が変化する。監査報告や再現性が必要な用途には、対象日を固定した物理マートまたはスナップショットを使用する。

`ON`句に期間条件を置くことだけで、パーティションプルーニングやBIの安全な実行が保証されるわけではない。実際のBI接続で検証する。

## 15. AI、ML、ベクトル

### 15.1 AI利用の最低管理要件

AIまたはMLに利用するデータは、ユースケース、目的、責任者、入力項目、学習・微調整・推論・RAGの区分、出力の利用先、影響を受ける人、モデル提供者、保存場所、ログ保持、削除方法を台帳化する。

個人情報、営業秘密、著作物、機密情報を外部AIサービスへ入力する場合は、利用目的、契約、学習利用、保存、再委託、削除、国外移転、アクセス制御を事前確認する（参考：[個人情報の保護に関する法律についてのガイドライン（通則編）](https://www.ppc.go.jp/personalinfo/legal/guidelines_tsusoku/)、[生成AIサービスの利用に関する注意喚起等について](https://www.ppc.go.jp/news/careful_information/230602_AI_utilize_alert/)、[AIと著作権について](https://www.bunka.go.jp/seisaku/chosakuken/aiandcopyright.html)）。ベクトル、チャンク、埋込み、検索ログ、プロンプト、回答は、原文と同じまたは適切な高い分類で管理し、ベクトル化だけで匿名化済みと判断してはならない。

個人の雇用、信用、価格、保険、医療、教育その他の重要な権利利益へ影響する用途では、人間による確認、異議申立て、救済、バイアス評価、性能監視、モデル変更管理を別途定義する（参考：[AI事業者ガイドライン（第1.2版）](https://www.meti.go.jp/shingikai/mono_info_service/ai_shakai_jisso/20260331_report.html)、[Regulation (EU) 2016/679 (GDPR)](https://eur-lex.europa.eu/eli/reg/2016/679/oj/eng)、[Regulation (EU) 2024/1689 (EU AI Act)](https://eur-lex.europa.eu/eli/reg/2024/1689/oj/eng)、[AI Risk Management Framework](https://www.nist.gov/itl/ai-risk-management-framework)）。

1. ML特徴量テーブルへ`feat_`プレフィックスを使用する。
2. 特徴量へ生成日時、対象時点、特徴量バージョンを付与する。
3. 学習時点より後の情報が混入するデータリーケージを検査する。
4. PIIは目的、必要性、モデルリスクを確認し、最小限だけ使用する。
5. ベクトルへモデル名、次元数、生成日時、入力データ識別子を記録する。
6. ベクトルをBI向けGoldへ直接公開しない。

## 16. 例外管理と本番公開チェックリスト

### 16.1 例外管理

例外には次を記録する。

- 対象資産
- 適用する規則
- 例外理由
- リスク評価
- 代替統制
- 所有者
- 承認者
- 有効期限
- 解消計画

期限のない恒久例外を作成してはならない。期限到来時に、継続、解消、規則改訂のいずれかを判断する。

### 16.2 本番公開チェックリスト

本番公開前には、次を確認する。

#### 16.2.1 文書と設計

- [ ] 文書オーナー、承認者、改訂責任者が登録されている。
- [ ] データオーナー、スチュワード、運用担当者が定義されている。
- [ ] 1行の粒度、主キー、更新方式、保持期間が定義されている。
- [ ] PII、機密区分、アクセス経路、エクスポート可否が定義されている。
- [ ] 下流資産と破壊的変更の影響が確認されている。

#### 16.2.2 実装

- [ ] 命名規則、説明文、型、単位が適合している。
- [ ] SQL、YAML、Terraformの構文検証が完了している。
- [ ] DDLをdry runし、テスト用データセットで実行している。
- [ ] dbtまたはDataformのcompileと品質テストが成功している。
- [ ] 同じ入力を再実行しても業務データが重複しない。
- [ ] 未解決プレースホルダーが存在しない。

#### 16.2.3 セキュリティ

- [ ] 一般利用者が原PIIへアクセスできない。
- [ ] PII列へPolicy Tagまたは代替制御が設定されている。
- [ ] 疑似識別子が制限された処理で生成されている。
- [ ] 暗号鍵、Secret、トークンがコードとログへ露出していない。
- [ ] BI、エクスポート、共有、コピーの経路を確認している。
- [ ] 監査ログとアラートが設定されている。
- [ ] IAMの特権ロールと一時権限がレビュー済みである。
- [ ] Organization Policyとデータ境界が適用済みである。
- [ ] 監査ログの中央集約、保持、削除防止が確認済みである。
- [ ] PII、機密、公開データの分類とPolicy Tagが確認済みである。
- [ ] Secretの所有者、ローテーション、漏洩時手順が登録済みである。
- [ ] CMEKの要否、鍵管理者、復旧方法が確認済みである。
- [ ] CI/CDの依存脆弱性、コンテナ、Secret scanが合格している。

#### 16.2.4 コストと運用

- [ ] パーティションとクラスタリングの効果を実測している。
- [ ] `require_partition_filter`の直接SQL、ビュー、BI、バックフィルを検証している。
- [ ] 鮮度、件数、品質、費用、失敗を監視できる。
- [ ] 障害時の公開停止、前回正常版、再処理手順がある。
- [ ] RPO/RTOと復元テスト結果がある。
- [ ] 予算、費用配賦、異常費用通知、停止手順が設定済みである。
- [ ] `maximum_bytes_billed`、Reservation、TTL、BI抽出費用が確認済みである。
- [ ] ネットワーク転送費とバックアップ費用が見積もり済みである。
- [ ] 問い合わせ先、エスカレーション先、Runbookが登録されている。

### 16.3 受入テスト

本書の統制が実装に適用されていることを、次の合格条件で確認する。

| 分類 | 合格条件 |
|:---|:---|
| 増分処理 | Dataform、dbt等の生成SQLが対象パーティションだけを走査する |
| バックフィル | 指定期間外の走査と書き込みが発生しない |
| 冪等性 | 同じ入力と処理版の再実行で重複・二重計上が発生しない |
| Slot | `total_slot_ms`、実行時間、キュー待機、シャッフル量を比較できる |
| Reservation | ELT、BI、MLの割り当て、リージョン、Editionを確認している |
| クラスタリング | 列順を実ワークロードで比較し、採用理由を説明できる |
| PII | Policy Tag、行制御、IAM、エクスポート経路を検証している |
| 疑似識別子 | 正規化、鍵バージョン、コンテキストが契約どおり一致している |
| マスキング | マスク済み表示列をJOINキーに使用していない |
| AI/ML | 入力データ、モデル、出力、権限、ログ、削除を追跡できる |
| Time Travel | RPO、課金モデル、保持期間、Fail-safe、バックアップを確認している |
| DR/BCP | RTO/RPOに基づきコード、データ、権限、鍵、依存を復旧できる |

## 17. セキュリティ・コスト統制

### 17.1 セキュリティ統制マトリクス

| ID | 統制領域 | 統制要件 | 適用対象 | 実装・検証方法 | 証跡 | 頻度・期限 | 所有者 | 重大度 |
|---|---|---|---|---|---|---|---|---|
| SEC-01 | 組織境界 | 開発・検証・本番をGCPプロジェクト単位で分離する | 全プロジェクト | Organization / Folder / Project構成レビュー | 構成図、Terraform plan | 環境追加時・年次 | 基盤責任者 | Critical |
| SEC-02 | 組織ポリシー | 未承認リージョン、サービス、外部共有、サービスアカウントキー作成を制限する | 組織・フォルダ・プロジェクト | Organization Policyの適用状況をCIまたは定期監査で確認 | Policy一覧、例外台帳 | 月次 | セキュリティ責任者 | Critical |
| SEC-03 | IAM | 人間、CI/CD、実行サービスアカウントを分離する | IAM全体 | IAMポリシーとサービスアカウント一覧を照合 | IAM export、差分レポート | 月次 | 基盤責任者 | Critical |
| SEC-04 | IAM | Owner、Editor、IAM管理者等の広範な権限を原則付与しない | プロジェクト・フォルダ | 高権限ロールの付与を自動検出 | IAMポリシー、承認記録 | 継続・月次棚卸し | セキュリティ責任者 | Critical |
| SEC-05 | IAM | 一時的な特権アクセスには期限と承認者を付与する | 特権アクセス | IAM Conditionsまたは承認ワークフローを使用 | 申請、承認、利用ログ | 付与ごと | システム所有者 | High |
| SEC-06 | IAM | 退職・異動・プロジェクト終了時に権限を削除する | 人間ユーザー | 人事・IdP情報とIAMを定期照合 | 棚卸し結果 | 日次または週次 | 情報システム部門 | Critical |
| SEC-07 | IAM | サービスアカウントの未使用・過剰権限を検出する | サービスアカウント | IAM Recommender等と実利用ログをレビュー | 変更前後の権限、レビュー記録 | 月次 | 基盤責任者 | High |
| SEC-08 | IAM | Break-glassアカウントを限定し、利用を必ず監査する | 緊急アクセス | 利用時通知、事後レビュー、資格情報保管 | 利用ログ、事後承認 | 利用時・四半期 | セキュリティ責任者 | High |
| SEC-09 | データ分類 | 新規データセット・テーブル・列に機密区分と所有者を登録する | 全データ資産 | CIで分類・所有者・期限の欠落を検査 | データカタログ、契約 | 作成・変更時 | データオーナー | Critical |
| SEC-10 | PII | 原PIIをRestricted領域へ分離し、一般BIから直接参照できないようにする | PII資産 | Policy Tag、IAM、認可ビュー、Row Access Policyを検証 | Policy Tag一覧、クエリテスト | リリースごと | データオーナー | Critical |
| SEC-11 | PII | 疑似識別子は限定ETLで事前生成し、鍵版・正規化版を管理する | 疑似識別子 | Gold SQLに原PIIと鍵参照がないことをCI検査 | 鍵版、変換仕様、監査ログ | 変更時 | セキュリティ責任者 | Critical |
| SEC-12 | PII | マスク済み列をJOINキーに使用しない | PII下流処理 | SQL静的検査と代表クエリテスト | 検査結果、テスト結果 | CIごと | データエンジニア | High |
| SEC-13 | PII | DLPまたは同等方式で新規データのPII混入を検査する | Bronze / Silver / Gold | サンプルスキャンと人手レビュー | スキャン結果、承認記録 | 資産作成時・四半期 | データスチュワード | High |
| SEC-14 | データアクセス | PII参照、エクスポート、コピー、共有、削除を監査する | BigQuery / GCS | Data Accessログとアラートを有効化 | ログ、検知ルール | 継続 | SOC / 運用責任者 | Critical |
| SEC-15 | エクスポート | 機密データのExtract、Copy、外部共有を申請・承認制にする | BigQuery / GCS / BI | ジョブログ、宛先、利用目的、期限を記録 | エクスポート台帳 | 実行ごと | データオーナー | High |
| SEC-16 | ネットワーク | BigQuery、GCS、Secret Manager等のアクセス境界を定義する | 本番データ | VPC Service Controls、Private Google Access等を評価・適用 | 境界構成、疎通テスト | 設計時・年次 | ネットワーク責任者 | Critical |
| SEC-17 | ネットワーク | Cloud Run / Functionsから外部APIへの通信先と出口を制限する | 外部連携 | VPC、Cloud NAT、Firewall、許可先リストを検証 | 設定、疎通テスト | 変更時 | 基盤責任者 | High |
| SEC-18 | 暗号化 | 機密区分に応じてGoogle-managed encryptionまたはCMEKを選択する | BQ / GCS / Pub/Sub等 | データ分類と暗号方式を照合 | KMS設定、分類台帳 | 資産作成時 | セキュリティ責任者 | High |
| SEC-19 | 鍵管理 | 鍵管理者とデータ利用者を分離し、ローテーション・失効・復旧を定義する | Cloud KMS | 鍵ポリシー、アクセスログ、復旧テストを確認 | 鍵台帳、テスト結果 | 月次監視・年次テスト | KMS管理者 | Critical |
| SEC-20 | Secret | Secret Version、ローテーション期限、所有者を管理する | Secret Manager | 未期限・未使用・旧Versionを検出 | Secret台帳、アクセスログ | 月次 | セキュリティ責任者 | High |
| SEC-21 | Secret | 漏洩時の失効、再発行、影響範囲確認、監査を定義する | APIキー等 | インシデント訓練または手順レビュー | Runbook、訓練記録 | 半期 | SOC / システム所有者 | Critical |
| SEC-22 | CI/CD | SQL、Terraform、Dataform、アプリの依存脆弱性を検査する | リポジトリ・ビルド | SCA、SAST、Secret scan、SQL lintをCI実行 | CIログ、脆弱性一覧 | Pull Requestごと | 開発責任者 | High |
| SEC-23 | コンテナ | イメージをDigest固定し、脆弱性スキャン・署名・承認を行う | Cloud Run / Functions | Artifact Registryスキャン、署名、デプロイ制御 | Image digest、スキャン結果 | ビルド・デプロイ時 | 開発責任者 | High |
| SEC-24 | CI/CD権限 | ビルド、検証、本番デプロイのサービスアカウントを分離する | Cloud Build / Deploy | IAMとパイプライン定義をレビュー | IAM、承認履歴 | リリースごと | DevOps責任者 | Critical |
| SEC-25 | 監査ログ | Admin Activity、Data Access、System Event、Policy Deniedを集約する | 全GCPサービス | Log Routerと専用ログ保存先を検証 | ログシンク、受信テスト | 月次 | SOC | Critical |
| SEC-26 | 監査ログ | 監査ログを通常運用者から分離し、保持ロック・削除防止を設定する | 監査プロジェクト / バケット | 削除権限と保持設定を検証 | IAM、保持設定 | 四半期 | セキュリティ責任者 | Critical |
| SEC-27 | スキーマ変更 | 破壊的変更、PII分類引上げ、権限変更をレビューする | BQ資産 | 依存関係・公開差分・移行計画をCI検査 | Change ticket、plan、承認 | 変更時 | データオーナー | High |
| SEC-28 | 削除 | 保持期限、削除要求、legal hold、派生資産への削除波及を管理する | 全データ資産 | 削除ワークフローと完了照合 | 削除台帳、実行ログ | 月次・要求時 | データオーナー | Critical |
| SEC-29 | インシデント | 漏洩、権限逸脱、ログ欠損、公開設定を検知・対応する | 全基盤 | 検知ルール、PagerDuty等、Runbook、訓練 | インシデント記録 | 継続・半期訓練 | SOC | Critical |
| SEC-30 | DR / BCP | データ、IAM、Policy Tag、鍵、BI、依存サービスを復元する | 本番基盤 | RPO / RTOテストを実施 | 復元テスト結果 | 半期または年次 | 基盤責任者 | Critical |

### 17.2 コスト統制マトリクス

| ID | 統制領域 | 統制要件 | 適用対象 | 実装・検証方法 | 証跡 | 頻度・期限 | 所有者 | 重大度 |
|---|---|---|---|---|---|---|---|---|
| COST-01 | 予算 | プロジェクト、環境、チーム、データプロダクト別に予算を設定する | 全GCP費用 | Billing Budgetと費用配賦を設定 | 予算設定、配賦表 | 月次見直し | FinOps責任者 | Critical |
| COST-02 | 予算 | 50%、80%、100%、120%などの通知閾値を定義する | Billing | Cloud Billing通知と担当者を設定 | 通知テスト | 設定時・四半期 | FinOps責任者 | High |
| COST-03 | 異常検知 | 前日、前週、前月、前年同日との費用差分を検知する | BQ / GCS / Compute / Network | Billing Exportを集計し、閾値アラート | 異常レポート | 日次 | FinOps / 運用 | High |
| COST-04 | 費用配賦 | `environment`、`domain`、`data_product`、`pipeline`、`owner`、`cost_center`を必須ラベルとする | Job / Resource | CIとBilling Exportで欠落検査 | ラベル欠落一覧 | リリース・日次 | プロダクト所有者 | High |
| COST-05 | クエリ上限 | 本番のユーザー・ジョブに`maximum_bytes_billed`等の上限を設定する | BigQuery Job | ジョブ設定と高額クエリを監査 | Job設定、失敗ログ | リリース・日次 | BQ管理者 | Critical |
| COST-06 | クエリ検証 | 代表クエリをdry runし、bytes processedとレイテンシをベースライン比較する | SQL / Dataform / BI | CI・ステージング負荷試験 | Baseline、CI結果 | PR・リリース時 | データエンジニア | High |
| COST-07 | 全期間走査 | 対象パーティション条件のない大規模クエリを拒否する | BQ / BI | `require_partition_filter`、静的検査、Job監視 | 拒否ログ、検査結果 | 継続 | BQ管理者 | Critical |
| COST-08 | バックフィル | 月次・長期バックフィルは事前見積もり、承認、分割、低優先度実行とする | BQ / Dataform | dry run、予約枠、実行時間帯を管理 | 承認、見積もり、Job履歴 | 実行ごと | データオーナー | High |
| COST-09 | Reservation | ELT、BI、ML、管理処理を必要に応じてReservation分離する | BigQuery | Slot使用量、待機時間、失敗率を評価 | Reservation設計、Job統計 | 月次 | BQ管理者 | High |
| COST-10 | Slot | ベースラインSlot、ピークSlot、キュー待機時間、未使用率を監視する | BigQuery | INFORMATION_SCHEMAとReservation統計 | Slotレポート | 日次・月次 | BQ管理者 | High |
| COST-11 | ストレージ | Bronze、Quarantine、Sandbox、ステージングのTTLを設定する | BQ / GCS | Partition expiration、Object Lifecycle | TTL設定、削除ログ | 設計時・月次 | データオーナー | Critical |
| COST-12 | 長期保存 | 長期保存料金、Time Travel、Fail-safe、更新による再課金を含めて評価する | BQ Storage | ストレージ課金と更新履歴を比較 | コスト分析 | 月次 | FinOps / BQ管理者 | High |
| COST-13 | 孤立資産 | 未使用テーブル、ビュー、バケット、Artifact、予約枠を検出する | 全GCP資産 | Query history、Asset Inventory、利用者確認 | 孤立資産一覧 | 月次 | FinOps | High |
| COST-14 | DML | 高頻度のMERGE、UPDATE、DELETE、小粒度DMLを抑制する | BigQuery | DML回数、更新パーティション、費用を監視 | DMLレポート | 日次 | データエンジニア | High |
| COST-15 | 取込 | 小さなファイル大量投入、再送、重複ストレージを抑制する | GCS / BQ Load / Streaming | ファイル数、サイズ、重複率、取込方式を監視 | 取込レポート | 日次 | 取込責任者 | High |
| COST-16 | BI | 抽出、ライブ接続、期間、粒度、更新頻度をデータ契約に定義する | BI / Serving | レポート別Job統計、抽出履歴を監査 | BI費用レポート | 月次 | BI所有者 | High |
| COST-17 | BI停止 | 未使用レポート、重複抽出、過剰更新を停止する | BI | 利用状況と費用をレビュー | 廃止記録 | 月次 | BI所有者 | Medium |
| COST-18 | ネットワーク | リージョン間転送、外部転送、NAT、クロスリージョンバックアップを監視する | Network / BQ / GCS | Billing Exportと転送量を照合 | 転送費レポート | 月次 | ネットワーク責任者 | High |
| COST-19 | 開発環境 | Sandbox、開発プロジェクト、予約枠、実行基盤を自動停止・期限削除する | Dev / Stg | TTL、Scheduler、未使用検出 | 停止・削除ログ | 日次 | 開発責任者 | High |
| COST-20 | 変更管理 | パーティション、クラスタ、マテビュー、Reservation変更を費用レビューする | IaC / BQ | Terraform planと費用影響見積もり | Change ticket | 変更時 | FinOps / 基盤 | High |
| COST-21 | 費用停止 | 異常費用時に停止可能な処理と停止禁止の重要処理を定義する | 全パイプライン | Runbook、手動・自動停止テスト | Runbook、訓練記録 | 半期 | 運用責任者 | Critical |
| COST-22 | 単位コスト | データプロダクト、行数、処理GB、利用者、API呼出し等の単位コストを計測する | 主要資産 | Billing Exportとメタデータを結合 | 単位コストKPI | 月次 | FinOps | Medium |

統制マトリクスの検査は12.2のCI/CDに、確認項目は16.2の本番公開チェックリストに組み込んでいる。

## 参考資料

* [Introduction to column-level access control | BigQuery](https://cloud.google.com/bigquery/docs/column-level-security-intro)
* [AEAD encryption functions | BigQuery](https://cloud.google.com/bigquery/docs/reference/standard-sql/aead_encryption_functions)
* [Introduction to partitioned tables | BigQuery](https://docs.cloud.google.com/bigquery/docs/partitioned-tables)
* [Introduction to row-level security | BigQuery](https://docs.cloud.google.com/bigquery/docs/row-level-security-intro)
* [Job resource | BigQuery REST API](https://cloud.google.com/bigquery/docs/reference/rest/v2/Job)
* [Introduction to clustered tables | BigQuery](https://cloud.google.com/bigquery/docs/clustered-tables)
* [Understand reservations | BigQuery](https://cloud.google.com/bigquery/docs/reservations-workload-management)
* [Data retention with time travel and fail-safe | BigQuery](https://cloud.google.com/bigquery/docs/time-travel)
* [個人情報の保護に関する法律についてのガイドライン（通則編）](https://www.ppc.go.jp/personalinfo/legal/guidelines_tsusoku/)
* [生成AIサービスの利用に関する注意喚起等について](https://www.ppc.go.jp/news/careful_information/230602_AI_utilize_alert/)
* [AIと著作権について](https://www.bunka.go.jp/seisaku/chosakuken/aiandcopyright.html)
* [AI事業者ガイドライン（第1.2版）](https://www.meti.go.jp/shingikai/mono_info_service/ai_shakai_jisso/20260331_report.html)
* [Regulation (EU) 2016/679 (GDPR)](https://eur-lex.europa.eu/eli/reg/2016/679/oj/eng)
* [Regulation (EU) 2024/1689 (EU AI Act)](https://eur-lex.europa.eu/eli/reg/2024/1689/oj/eng)
* [AI Risk Management Framework](https://www.nist.gov/itl/ai-risk-management-framework)
* [Organization Policy Service documentation](https://cloud.google.com/security/organization-policy)
* [VPC Service Controls overview](https://cloud.google.com/vpc-service-controls/docs/overview)
* [Cloud Audit Logs overview](https://cloud.google.com/logging/docs/audit)
* [Create, edit, or delete budgets and budget alerts](https://cloud.google.com/billing/docs/how-to/budgets)

---

**運用開始条件**：組織のセキュリティ責任者、基盤責任者、データ責任者、FinOps責任者が、統制の適用範囲、例外、証跡、運用担当を承認する。正式適用にあたっては、組織の承認記録、対象GCP環境、利用ツールのバージョン、法令、社内セキュリティ基準を確認する。本書は改訂可能な管理文書であり、将来の製品仕様や業務要件の変更を妨げない。
